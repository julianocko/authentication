version: "3.9"

services:
  jwt-assertion-service:
    build: .
    container_name: jwt-assertion-service
    ports:
      - "3000:3000"
    volumes:
      - ./keys:/app/keys:ro
    restart: unless-stopped
